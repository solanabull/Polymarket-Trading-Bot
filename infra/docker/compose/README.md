# Docker Compose (Phase 3+)

Phase 3 will add:

- services for each bot implementation (rust core, ts control plane, py research)
- a local persistence database (SQLite/Postgres)
- metrics/monitoring stack (Prometheus-compatible)

This file is a placeholder to keep the monorepo structure aligned with the planned implementation phases.

