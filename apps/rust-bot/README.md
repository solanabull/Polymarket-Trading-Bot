# Rust Bot (Phase 1-2)

This crate defines:

- shared message contracts (`OrderIntent`, `StrategyOutput`, `RiskDecision`, etc.)
- the shared strategy interface (`Strategy` trait)

No Polymarket connectivity is implemented until Phase 3.

