# Sample Replay Dataset

This folder contains minimal example WebSocket-derived events in NDJSON format to support:

- deterministic replay plumbing
- snapshot/delta orderbook replay tests

In Phase 3+, these logs are replayed through the market-data normalization + orderbook state manager.

